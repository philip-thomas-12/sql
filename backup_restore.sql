-- Backup (can be exported from tools like phpMyAdmin or MySQL Workbench)
-- Restore via terminal:
-- mysql -u username -p database_name < backup.sql

-- Example: Export command
-- mysqldump -u root -p Company > company_backup.sql
